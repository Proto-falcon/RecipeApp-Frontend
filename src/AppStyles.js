import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	searchIcon: {
		height: 20,
		width: 20,
		resizeMode: "contain",
		marginRight: 10
	},

	pageContainer: {
		marginTop: 10
	}
});