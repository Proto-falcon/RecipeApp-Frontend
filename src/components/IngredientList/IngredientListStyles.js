import { StyleSheet } from "react-native";

export const igStyles = StyleSheet.create({
    ingredient: {
        padding: 10,
        borderWidth: 1,
        marginBottom: 10,
        flexGrow: 1,
        minHeight: "12%"
    }
});