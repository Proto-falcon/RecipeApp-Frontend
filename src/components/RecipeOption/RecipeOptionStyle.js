import { StyleSheet } from "react-native";

export const optiionStyles = StyleSheet.create(
    {
        listsContainer: {
            flexDirection: "row"
        },

        list: {
            marginTop: 10
        },
    });