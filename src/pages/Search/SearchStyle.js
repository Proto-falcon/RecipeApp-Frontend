import { StyleSheet } from "react-native";

export const SearchStyle = StyleSheet.create(
    {
        container: {
            flex: 2,
            alignItems: "center",
        },

        imgContainer: {
            margin: 7
        },
    });