import { StyleSheet } from "react-native";

export const SearchStyle = StyleSheet.create(
    {
        container: {
            alignItems: "center",
        },

        imgContainer: {
            margin: 7
        },
    });